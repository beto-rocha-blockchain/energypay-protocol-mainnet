# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*
backend/.env
backend/.env.*
!.env.example
!**/.env.example
!.env.production

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Pitch decks / private keys — keep out of the source repo
*.pptx
*.key

# Local REST-client scratch (commit the backend/test.http.example template instead)
backend/test.http
*.local.http

# Keep the generated Stellar Testnet settlement evidence (SCF Deliverable 2),
# overriding the broad *.log rule above so the rail logs are reviewable in-repo.
!scripts/testnet-evidence/
!scripts/testnet-evidence/*.log
